# Backend Server Configuration
# Port for local development server
PORT=4000

# Environment
# Set to '1' when deployed on Vercel (automatically set by Vercel)
# VERCEL=1

# CORS Configuration
# Allowed origins for CORS (comma-separated for multiple origins)
# Current config: allows all origins with '*'
# For production, specify exact origins: CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
# CORS_ORIGINS=*

# Node Environment
NODE_ENV=development
